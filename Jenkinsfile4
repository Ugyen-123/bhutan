pipeline {
    agent any

    environment {
        APP_NAME = "payment-app"
        VERSION = "1.0"
    }

    stages {

        stage('Display Variables') {
            steps {
                echo "Application: ${APP_NAME}"
                echo "Version: ${VERSION}"
            }
        }
    }
}
